"""
geophyto_qa.audit — dataset integrity checks.

    check_splits — assert no Bugwood image_number spans two splits (image-level
                   leakage would inflate held-out generalization claims).
"""
